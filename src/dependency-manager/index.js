const WiretreeImplementation = require('./implementations/wiretree-adapter');

// Export the implementation of your choice;
// Wiretree has proven perfect and fast so far.
module.exports = WiretreeImplementation;
